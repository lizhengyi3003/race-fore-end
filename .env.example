# ===== 前端环境变量示例 =====
# 复制为 .env.development / .env.production 后按需修改（.env*.local 不入库）

# 后端 API 前缀（开发环境走 Vite 代理时填 /api/v1）
# VITE_API_BASE_URL=/api/v1

# 开发代理目标（Vite dev server 将 /api 代理到该地址，默认 http://localhost:8000）
# VITE_DEV_PROXY=http://localhost:8000

# 生产环境说明：
# 未配置 VITE_API_BASE_URL 时，src/api/index.ts 会按部署域名自动分流：
#   *.workers.dev 预览（dev 分支）→ https://api.intellicoretech.cn/dev/api/v1
#   自定义域名 intellicoretech.cn（main 生产）→ https://api.intellicoretech.cn/api/v1
# 如需强制固定后端地址，取消下面注释：
# VITE_API_BASE_URL=https://api.intellicoretech.cn/api/v1
