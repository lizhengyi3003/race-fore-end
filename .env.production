# 前端环境变量（生产环境）
# baseURL 由 src/api/index.ts 按部署环境自动分流：
#   *.workers.dev 预览(dev) → https://api.intellicoretech.cn/dev/api/v1
#   intellicoretech.cn(main) → https://api.intellicoretech.cn/api/v1
# 如需强制覆盖，取消下面注释：
# VITE_API_BASE_URL=https://api.intellicoretech.cn/api/v1
